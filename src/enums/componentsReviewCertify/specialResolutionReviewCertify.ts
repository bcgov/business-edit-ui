/**
 * List of components on Special Resolution Review and Certify page. Note:
 * - these values MUST match component IDs to scroll correctly
 * - order this according to component layout
 * - this list must match `SpecialResolutionFlagsReviewCertifyIF`
 */
export enum SpecialResolutionReviewCertify {
  'special-resolution-confirm',
  'document-delivery-section',
  'transactional-folio-number-section',
  'completing-party-section',
  'certify-section',
  'staff-payment'
}
