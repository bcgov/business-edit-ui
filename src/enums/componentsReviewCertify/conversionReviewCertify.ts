/**
 * List of components on Conversion Review and Certify page. Note:
 * - these values MUST match component IDs to scroll correctly
 * - order this according to component layout
 * - this list must match `ConversionFlagsReviewCertifyIF`
 */
export enum ConversionReviewCertify {
  'completing-party-section'
}
