/**
 * Enum for alteration Company Info component validator flags.
 * Enum constants MUST match component ID to scroll correctly.
 * Order should follow component layout.
 * */
export enum AlterationCompanyInfoFlags {
  'company-name-section',
  'business-type',
  'name-translation',
  'contact-info',
  'folio-number',
  'share-structures',
  'articles'
}
