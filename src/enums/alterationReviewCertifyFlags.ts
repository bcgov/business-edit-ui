/**
 * Enum for alteration Review and Certify component validator flags.
 * Enum constants MUST match component ID to scroll correctly.
 * Order should follow component layout.
 * */
export enum AlterationReviewCertifyFlags {
  'effective-date-time',
  'document-delivery-section',
  'certify-section',
  'court-order',
  'staff-payment'
}
