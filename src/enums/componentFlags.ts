/**
 * Enum for component validator flags.
 * enum properties MUST match component ID to scroll correctly.
 * Order is arbitrary but follows component hierarchy.
 * */
export enum ComponentFlags {
  'articles',
}
